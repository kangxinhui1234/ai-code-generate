<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codegenerate.aicodegenerate.mapper.ChatHistoryMapper">

    <!-- 根据应用ID查询对话历史（按时间降序） -->
    <select id="selectByAppIdOrderByCreateTimeDesc" resultType="com.codegenerate.aicodegenerate.entity.ChatHistory">
        SELECT id, message, messageType, appId, userId, createTime, updateTime, isDelete
        FROM chat_history
        WHERE appId = #{appId}
          AND isDelete = 0
        ORDER BY createTime DESC
        <if test="limit != null and limit > 0">
            LIMIT #{limit}
        </if>
    </select>

    <!-- 根据应用ID分页查询对话历史（按时间降序） -->
    <select id="selectByAppIdWithPageOrderByCreateTimeDesc" resultType="com.codegenerate.aicodegenerate.entity.ChatHistory">
        SELECT id, message, messageType, appId, userId, createTime, updateTime, isDelete
        FROM chat_history
        WHERE appId = #{appId}
          AND isDelete = 0
        ORDER BY createTime DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 查询所有对话历史（按时间降序）- 管理员使用 -->
    <select id="selectAllOrderByCreateTimeDesc" resultType="com.codegenerate.aicodegenerate.entity.ChatHistory">
        SELECT id, message, messageType, appId, userId, createTime, updateTime, isDelete
        FROM chat_history
        WHERE isDelete = 0
        ORDER BY createTime DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 根据应用ID删除对话历史 -->
    <update id="deleteByAppId">
        UPDATE chat_history
        SET isDelete = 1, updateTime = NOW()
        WHERE appId = #{appId}
          AND isDelete = 0
    </update>

    <!-- 统计应用的对话历史数量 -->
    <select id="countByAppId" resultType="long">
        SELECT COUNT(*)
        FROM chat_history
        WHERE appId = #{appId}
          AND isDelete = 0
    </select>

</mapper>
